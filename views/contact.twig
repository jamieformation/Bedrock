{% extends 'base.twig' %}

{% block content %}

  <section class="page contact-page">
    <div class="container">
      <h1>{{ post.title }}</h1>
    </div>
    <div class="container">
      {{ post.content }}
    </div>
  </section>

{% endblock %}
